 //int main() {
  #ifndef ONLINE_JUDGE
    freopen("in.txt", "r", stdin); //in.txt name can be changed
    freopen("out.txt", "w", stdout); //in.txt name can be changed
  #endif

  //put at the begining of an int main()
  //to get input and output from .txt
  // input, output and the using file must be in same directory
